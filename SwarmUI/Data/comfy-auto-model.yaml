swarmui:
    base_path: D:\SwarmUI\Models
    is_default: true
    checkpoints: |
       Stable-Diffusion
    vae: |
       VAE
    loras: |
       Lora
       LyCORIS
    upscale_models: |
       ESRGAN
       RealESRGAN
       SwinIR
       upscale-models
       upscale_models
    embeddings: |
       Embeddings
    hypernetworks: |
       hypernetworks
    controlnet: |
       controlnet
       model_patches
    model_patches: |
       controlnet
       model_patches
    clip: |
       text_encoders
       clip
    clip_vision: |
       clip_vision
    unet: |
       unet
    diffusion_models: |
       diffusion_models
    gligen: |
       gligen
    ipadapter: |
       ipadapter
    yolov8: |
       yolov8
    tensorrt: |
       tensorrt
    clipseg: |
       clipseg
    style_models: |
       style_models
    latent_upscale_models: |
       latent_upscale_models

# Explicitly separate the _nodes list to prevent it from being is_default
swarmui_nodes:
    custom_nodes: |
       D:\SwarmUI\src\BuiltinExtensions\ComfyUIBackend\DLNodes
       D:\SwarmUI\src\BuiltinExtensions\ComfyUIBackend\ExtraNodes
